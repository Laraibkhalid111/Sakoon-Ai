# Free-tier stack. Pin to major.minor ranges for reproducible installs.
# Docker/CI supported (see Dockerfile, docker-compose.yml, .github/workflows).

# Core Streamlit app shell
streamlit>=1.38.0,<2.0.0          # st.audio_input requires >=1.38

# Groq API (LLM chat + Whisper STT)
groq>=0.9.0,<2.0.0

# PDF report generation (pure Python, no system deps like wkhtmltopdf)
fpdf2>=2.7.9,<3.0.0

# Load .env for local scripts / fallback (Streamlit uses st.secrets natively)
python-dotenv>=1.0.0,<2.0.0

# Tests (local / CI)
pytest>=8.0.0,<9.0.0

# Auth + optional journal encryption (Phase 5)
bcrypt>=4.0.0,<5.0.0
cryptography>=42.0.0,<46.0.0
